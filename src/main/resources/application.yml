server:
  port: 8080

spring:
  application:
    name: querydsl
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: create

  datasource:
    hikari:
      driver-class-name: org.h2.Driver
      jdbc-url: jdbc:h2:tcp://localhost/./queryDSL
      username: sa
      password:
